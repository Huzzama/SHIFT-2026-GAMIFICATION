# FARO backend (not used by the prototype: the extension runs on mock data)
VITE_FARO_API_URL=http://localhost:3000

# Backend-only values. Never put these in the extension build.
# CANVAS_API_URL=
# CANVAS_CLIENT_ID=
# CANVAS_CLIENT_SECRET=
# DATABASE_URL=
# AI_API_KEY=
